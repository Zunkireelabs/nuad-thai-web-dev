server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html/;
    index index.html;

    # No cache for HTML files - ensures deploys show up immediately
    location ~* \.html$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires 0;
    }

    # No cache for RSC payload files (.txt) - needed for client-side navigation
    location ~* \.txt$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Content-Type "text/x-component";
    }

    # Long cache for Next.js hashed assets — filenames include content hash,
    # so a redeploy produces a new path and the cache is naturally invalidated.
    location ~* ^/_next/static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Short cache for unhashed static assets (images at /images/, fonts, audio).
    # These keep the same path across deploys, so we need browsers to revalidate
    # quickly. ETags still allow 304 responses, so the bandwidth hit is small.
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot|glb|gltf|hdr|mp3)$ {
        expires 5m;
        add_header Cache-Control "public, must-revalidate";
    }

    # SPA Fallback with RSC support
    # When Next.js router sends RSC header, serve .txt instead of .html
    location / {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";

        # Serve RSC payload for client-side navigation
        if ($http_rsc = "1") {
            rewrite ^/$ /index.txt last;
            rewrite ^(.+)/$ $1/index.txt last;
            rewrite ^(.+)$ $1.txt last;
        }

        try_files $uri $uri/ $uri.html /index.html;
    }

    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript application/octet-stream text/x-component;
}
